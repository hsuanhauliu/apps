# Apps

This repository contains a collection of my pure-frontend apps that run entirely in your browser, because why pay or give away your data for free when you can build it yourself?

See https://hsuanhauliu.github.io/apps/.

## Dev

Only files within the `docs/` folders are published.

Use `python3 generate_index_html.py` to update `index.html` when adding or removing apps.
